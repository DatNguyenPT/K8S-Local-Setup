apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: lease-role
  namespace: db-backup
rules:
  - apiGroups: [""]
    resources: ["leases"]
    verbs: ["get", "watch", "list", "update", "patch", "create"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
